export interface Environment {
    production: boolean;
    api: {
      home: string;
      // reservas: string;
      // livros: string;
      // emprestimos: string;
      // usuarios: string;
      // relatorios: string;
    };
  }
  